# OS
.DS_Store

# Large raw dataset (not tracked; see rebuild/README.md for download)
Lending Club Dataset/

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
rebuild/.venv/

# Rebuild data + model artifacts (regenerated by `make train`)
rebuild/data/raw/
rebuild/data/processed/
rebuild/models/*.pkl
rebuild/models/*.json

# Demo scorer models — tracked via Git LFS for Streamlit Cloud
!rebuild/models/pd_model.pkl
!rebuild/models/lgd_model.pkl
!rebuild/models/return_model.pkl
!rebuild/models/economics.json


# Legacy capstone artifacts (165 MB of pickled models; kept locally, untracked)
legacy_capstone/models/*.pkl
